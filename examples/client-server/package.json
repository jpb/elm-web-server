{
  "name": "client-server",
  "version": "1.0.0",
  "description": "An example of sharing elm code between client & server",
  "scripts": {
    "start": "node build/server.js",
    "build": "webpack -p --progress",
    "develop": "webpack --progress && webpack -w & nodemon build/server.js",
    "setup": "rm -rf build elm-stuff node_modules && npm i -y && elm-package install -y && webpack --progress"
  },
  "keywords": [],
  "author": "asger nielsen <opvasger@gmail.com>",
  "license": "BSD3",
  "devDependencies": {
    "elm-webpack-loader": "4.3.1",
    "nodemon": "1.11.0",
    "webpack": "2.6.1",
    "elm-http-server": "2.1.0"
  }
}
